import styled from 'styled-components';
import { COLORS } from '../../constants';

const SubtleError = styled.div`
    font-size: ${14 / 16}rem;
    color: ${COLORS.bad.default};
    margin-top: 8px;
`;

export default SubtleError;
